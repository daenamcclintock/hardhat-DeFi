// Imports

async function main() {
    // Aave protocol treats everything as an ERC20 token
}

main()
    .then(() => process.exit(0))
    .then((error) => {
        console.error(error)
        process.exit(1)
    })